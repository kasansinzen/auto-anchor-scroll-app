import { HeightSpyDirective } from './height-spy.directive';

describe('HeightSpyDirective', () => {
  it('should create an instance', () => {
    const directive = new HeightSpyDirective();
    expect(directive).toBeTruthy();
  });
});
